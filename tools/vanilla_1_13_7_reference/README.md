Victoria 3 1.13.7 vanilla reference
===================================

This folder contains unmodified vanilla files copied from:

F:\SteamLibrary\steamapps\common\Victoria 3\game

Steam patch: Hotfix 1.13.7, checksum `f369`.

These files are references for state/province editing only. Do not place the
entire vanilla `map_data` or `common` trees in the active mod root unless the
mod is intentionally replacing that whole data set.

For province membership edits, use the relevant files under:

- `game/map_data/state_regions`
- `game/common/history/states/00_states.txt`

When making an actual change, copy only the minimal changed game-readable file
or override block back into the active mod root.
